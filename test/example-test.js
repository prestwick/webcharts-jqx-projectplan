var describe,
    example,
    it,
    expect;

describe('sumAndDif', function () {
    'use strict';
    var result = example(1, 2);

    it('Should Sum', function () {
        expect(result.sum).toBe(3);
    });
});
